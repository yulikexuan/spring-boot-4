Hi there, could you inspect the `./prompt/flashcard_requirement.md` file, and then generate a new **prompt** to create an implementation plan from this file? 

Hi, I reviewed the new implementation plan you just created, and I would like to give more comments: 1. make `Long` be the primary key data type for `Flashcard` 2. `@GenerateLenses` and `Spring Data JDBC` are all settle down. 3. You can inspect entity `Beer` how to
implement `updateWith/patchWith` for `Flashcard` 4. The base path for the new REST Encpoints of `Flashcard` is `/sfg7/api/v1/flashcard` 5. Please regenerate the implementation file with name `flashcard_spec.md` into folder `./.prompt`, thank you!
